# Contributing

The best contribution is a small fixture for an environment-variable access pattern that `envproof` misses or misreads. Include the language, framework, expected variable name, and a regression test. Run `npm test` before submitting.

Never commit real environment values or secrets, even in test data.
